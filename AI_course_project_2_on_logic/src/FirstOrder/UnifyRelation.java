package FirstOrder;
import java.util.ArrayList;

public class UnifyRelation
{
	/*
    public static Substitutions theta;

	 
	
    public static Subst UnifyVar(Parameter p,Parameter o,Substitutions theta)
    {
        if (varIsInTheta(p,theta))
        {
            return unify(returnValueOfVars(p,theta), o, theta);
        }
        else if(varIsInTheta(o,theta))
        {
            return unify(p, returnValueOfVars(o,theta), theta);
        }
        else 
        {
            Subst substitution=new Subst(p,o);
            theta.add(substitution);
            return substitution;
        }
    }
	
	
	public static Subst unify(Object a,Object b,ArrayList<Subst> theta)
	{}
	


	//eksetasei an ena adikeimeno einai variable h oxi
	public static boolean isVariable(Object o)
	{
		if(o instanceof Parameter)
		{
			return true;
		}
		else
		{
			return false;
		}
	}
	
	
	*/
	
	
}//end class




