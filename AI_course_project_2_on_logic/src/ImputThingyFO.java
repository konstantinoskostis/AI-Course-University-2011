/**
 * @(#)ImputThingyFO.java
 *
 *
 * @author 
 * @version 1.00 2011/12/30
 */
import java.io.*;
import java.util.Scanner;
import java.util.Vector;
import FirstOrder.Relation;;

public class ImputThingyFO {
	/*Scanner da_thingyFO;
	Scanner linerFO;
	// pros_apodiksiFO;
	Vector<String> keimeno;
	String [] grammes;

    public ImputThingyFO(FileInputStream file) 
    { da_thingyFO = new Scanner(file);
      Scanner linerFO = da_thingyFO.useDelimiter(".\n"); //isws xriastei allagi tou delimiter an dn katalavei to ".\n"
    }
    
    public String ReadLine()
    {	if(linerFO.hasNext())
    	return linerFO.next();
    	else return null;
    }
    
    public String[] grammes()
    {keimeno = new Vector<String>();
     while (linerFO.hasNext())
     {keimeno.add(linerFO.next());
     }
     grammes = new String[keimeno.size()];
     for (int i=0; i<keimeno.size(); i++)
     {grammes[i] = keimeno.get(i);
     }
     return grammes;
    }
    
    public Object textparser(String txt, Object o)
    {Scanner p = new Scanner(txt);
     o = null;
     if (p.hasNext(":="))
     {
     }
     else 
     {
     }
     return o;
    }
    
    public Relation CreateRel(String txt)//sto text tha mpainei akrivws ena (kati(x,y,..z))
    {Scanner p = new Scanner(txt);
     String name;
     int ar=0;
     boolean valueIn = true;
     p = p.useDelimiter("(");
     name = p.next();
     while (p.hasNext("[a-z]"))//isws xriastei n' apomonosw to (x,y...z) mesa sto scanner
     { ar++;
     }
     Relation r = new Relation(name,ar,valueIn);
     //p = new Scanner(txt);
     //String values
     return r;
    }
    
    */
    
    
}